# Gemini API Key (Required for default AI features)
VITE_GEMINI_API_KEY=

# OpenAI API Key (Optional, used as a fallback if Gemini is saturated)
VITE_OPENAI_API_KEY=

# Anthropic API Key (Optional, used as a fallback if Gemini is saturated)
VITE_ANTHROPIC_API_KEY=
